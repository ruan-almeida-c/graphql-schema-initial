
type Query{
  ola:String
  horaAtual: Date
  usuarioLogado: Usuario
  produtoComprado: Produto
  numeroMegaSena: [Int!]!
  usuarios: [Usuario]
  usuario(id: Int): Usuario
  perfis: [Perfil]
  perfil(id: Int): Perfil
}