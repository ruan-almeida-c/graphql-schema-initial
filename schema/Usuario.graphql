
type Perfil{
  id: Int!
  nome: String!
}

enum usuarioStatus{
  ATIVO
  INATIVO
  BLOQUEADO
}

type Usuario{
  id: Int
  nome: String!
  email: String!
  idade: Int
  salario: Float
  vip: Boolean,
  perfil: Perfil
  status: usuarioStatus
}